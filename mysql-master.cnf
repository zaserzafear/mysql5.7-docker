[mysqld]
server-id              = $SERVER_ID_REPLACE
log_bin                = /var/log/mysql/mysql-bin.log
binlog_format          = row
expire_logs_days       = 10
max_binlog_size        = 100M

# MYSQL_REPLICAS_DB
# Replicate